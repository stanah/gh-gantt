---
name: gh-gantt-decompose
description: 要望を調査・分解して適切な粒度で Issue 化する。「X を実装して」「タスク化して」「Issue を作って」で使用。既存タスクとの重複・矛盾チェック、粒度判断、親子/依存関係の設定を行う。新機能の要件追加は gh-gantt-living-documentation、開発サイクル全体は gh-gantt-workflow を使うこと。
---

# gh-gantt Decompose

要望をそのまま Issue にせず、調査・分析してから適切な粒度でタスク化する。

<HARD-GATE>
既存タスクとの重複・矛盾チェックなしに Issue を作成してはならない。

チェック条件: 最新状態で `gh-gantt list` を実行し既存タスクを調査する（同一会話内で pull 済みなら再 pull 不要）。
失敗時: 重複・類似タスクがある場合、ユーザーに提示して方針を確認する（既存タスクの更新 / 新規作成 / 中止）。
Evidence: `gh-gantt list` の出力、および重複なしの判断根拠を提示する。
</HARD-GATE>

## プロセス

1. **同期（取得）** — `gh-gantt-sync`（pull）を invoke してリモートの最新状態を取得する。同一会話内で既に pull 済みなら省略可
2. **要件の参照元を確認** — **REQUIRED:** `.gantt-sync/workflow.md` を読み、タスク化の手順・ルール・body テンプレートの有無を確認する。ファイルが存在しない場合はスキップ可
3. **既存タスク調査** — `gh-gantt list` で重複・類似タスクを検索 — evidence として結果を提示
4. **矛盾チェック** — 既存タスクと矛盾する要望ではないか確認
5. **粒度の判断** — エピック / フィーチャー / タスクのどのレベルか。ユーザーに提示して確認
6. **分解** — 大きすぎる要望は適切な粒度に分割
7. **テンプレート適用** — タスクタイプに応じたテンプレートで body を構造化する。手順: [template-resolution.md](references/template-resolution.md)。**`## 完了条件` セクションは必ず `- [ ]` チェックボックス形式で書く** (進捗トラッキングのため、後述の規約参照)
8. **Issue 作成** — `gh-gantt create` で作成
9. **作成確認** — `gh-gantt show <number>` で作成内容を確認 — evidence として出力を提示
10. **構造化** — `gh-gantt link --set-parent` で親子関係を設定。**OPTIONAL:** `gh-gantt-dependencies` で依存関係を設定
11. **要件の追加判断** — プロジェクトが Living Documentation 体系を採用している場合（`.gantt-sync/workflow.md` に Living Documentation セクションがある）、振る舞い要件 (FR/NFR) を追加すべきタスクか判断する。**OPTIONAL:** 追加すべきなら `gh-gantt-living-documentation` を invoke して要件ファイルに AC を追加する
12. **同期** — `gh-gantt-sync`（push）を invoke して GitHub に反映する

## 完了条件 (Issue body) と要件 AC は別概念

`## 完了条件` (Issue body のチェックリスト) は **このタスクを完了するための作業項目**で、Issue close で役目を終える。
一方 `requirements.yaml` の `acceptance_criteria` (FR/NFR-AC) は **プロダクトが満たすべき不変の振る舞い契約**で、プロダクトが存在する限り維持される (Living Documentation Layer 1)。

両者を混同しないこと:

| 観点     | `## 完了条件` (Issue body)                               | `requirements.yaml` の AC                                        |
| -------- | -------------------------------------------------------- | ---------------------------------------------------------------- |
| 寿命     | Issue close で完了                                       | プロダクト存続中は不変                                           |
| 粒度     | 1 タスクの作業項目                                       | プロダクト全体の振る舞い契約                                     |
| 例       | 「`X` コマンドが動く」「テスト追加」「ドキュメント更新」 | 「ローカル・リモート両方が変更した場合にコンフリクトを検出する」 |
| 検証手段 | チェックボックスを ✓ する                                | テストが covered になる (`req:trace` 自動更新)                   |

`## 完了条件` の項目は **`- [ ]` チェックボックス形式**で書くこと。AI エージェントと人間の双方が body を機械的にパースして進捗をトラッキングできる。GitHub UI でもインタラクティブにチェックできる。

```markdown
## 完了条件

- [ ] 〜 が 〜 できる
- [ ] 〜 のテストが追加されている
- [x] (完了済の項目はこのように)
```

進捗確認は `gh issue view <N>`、`gh-gantt show <N>`、または GitHub Web UI で。

## Red Flags

| やりがちなこと                                           | 問題                                             |
| -------------------------------------------------------- | ------------------------------------------------ |
| 要望をそのまま 1 Issue にする                            | 粒度がばらける                                   |
| 既存タスクを調べない                                     | 重複・矛盾が生じる                               |
| body を空にする                                          | 要件が不明確なタスクが増える                     |
| 親子関係を設定しない                                     | 構造が見えない、進捗集計できない                 |
| テンプレートのガイドを無視する                           | 構造化の意味がなくなる                           |
| `## 完了条件` を散文や `- ` 箇条書きで書く               | 進捗のトラッキングができず AI が機械的に扱えない |
| `## 完了条件` と要件 AC を混同する                       | 粒度・寿命が違うため設計が破綻する               |
| 新機能なのに要件ファイルを更新しない (Living Doc 採用時) | Living Documentation のトレーサビリティが欠ける  |

| 言い訳                               | 現実                                                     |
| ------------------------------------ | -------------------------------------------------------- |
| 「タスクリストが長くて探すのが大変」 | `gh-gantt list` で検索できる。手間を惜しむと重複が増える |
| 「後で整理する」                     | 後では来ない。作成時に構造化する                         |
| 「小さいから body は不要」           | 3 ヶ月後の自分は覚えていない                             |
| 「このステップは該当しなさそう」     | 確認した上で「該当なし」と判断するのとスキップは違う     |
| 「テンプレートはないだろう」         | 思い込みで飛ばさない。Step 7 の手順に従い確認する        |

## リファレンス

- コマンド詳細: [commands.md](../gh-gantt-workflow/references/commands.md)
