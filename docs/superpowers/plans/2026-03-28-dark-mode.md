# Dark Mode Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** gh-gantt UI に GitHub Dark 準拠のダークモードを追加し、System / Light / Dark の3段階切り替えを提供する。

**Architecture:** CSS 変数の `[data-theme="dark"]` オーバーライドで配色を切り替える。React Context (`ThemeProvider`) で状態管理し、`localStorage` で永続化。FOUC 防止のため `index.html` にインラインスクリプトを配置。

**Tech Stack:** React Context, CSS Custom Properties, localStorage, matchMedia API, lucide-react icons

**Spec:** `docs/superpowers/specs/2026-03-28-dark-mode-design.md`

---

## File Structure

| File                                                 | Responsibility                                       |
| ---------------------------------------------------- | ---------------------------------------------------- |
| `packages/ui/index.html`                             | `[data-theme="dark"]` CSS 変数 + FOUC 防止スクリプト |
| `packages/ui/src/contexts/ThemeContext.tsx`          | ThemeProvider + useTheme hook                        |
| `packages/ui/src/components/toolbar/ThemeToggle.tsx` | テーマ切り替えアイコンボタン                         |
| `packages/ui/src/components/toolbar/Toolbar.tsx`     | ThemeToggle 配置                                     |
| `packages/ui/src/App.tsx`                            | ThemeProvider でラップ                               |
| `packages/ui/src/__tests__/theme.test.tsx`           | ThemeProvider テスト                                 |

---

### Task 1: ThemeContext — テーマ状態管理

**Files:**

- Create: `packages/ui/src/contexts/ThemeContext.tsx`
- Create: `packages/ui/src/__tests__/theme.test.tsx`

- [ ] **Step 1: テスト作成**

```tsx
// packages/ui/src/__tests__/theme.test.tsx
import { describe, expect, it, vi, beforeEach, afterEach } from "vitest";
import React from "react";
import { render, act } from "@testing-library/react";
import { ThemeProvider, useTheme } from "../contexts/ThemeContext.js";

function TestConsumer() {
  const { theme, resolvedTheme, setTheme } = useTheme();
  return (
    <div>
      <span data-testid="theme">{theme}</span>
      <span data-testid="resolved">{resolvedTheme}</span>
      <button data-testid="set-dark" onClick={() => setTheme("dark")}>
        Dark
      </button>
      <button data-testid="set-light" onClick={() => setTheme("light")}>
        Light
      </button>
      <button data-testid="set-system" onClick={() => setTheme("system")}>
        System
      </button>
    </div>
  );
}

describe("ThemeProvider", () => {
  let matchMediaListeners: Array<(e: { matches: boolean }) => void>;

  beforeEach(() => {
    localStorage.clear();
    document.documentElement.removeAttribute("data-theme");
    matchMediaListeners = [];
    vi.stubGlobal(
      "matchMedia",
      vi.fn().mockImplementation((query: string) => ({
        matches: false,
        media: query,
        addEventListener: (_: string, cb: (e: { matches: boolean }) => void) => {
          matchMediaListeners.push(cb);
        },
        removeEventListener: vi.fn(),
      })),
    );
  });

  afterEach(() => {
    vi.restoreAllMocks();
  });

  it("defaults to system theme", () => {
    const { getByTestId } = render(
      <ThemeProvider>
        <TestConsumer />
      </ThemeProvider>,
    );
    expect(getByTestId("theme").textContent).toBe("system");
    expect(getByTestId("resolved").textContent).toBe("light");
  });

  it("reads initial theme from localStorage", () => {
    localStorage.setItem("gh-gantt-theme", "dark");
    const { getByTestId } = render(
      <ThemeProvider>
        <TestConsumer />
      </ThemeProvider>,
    );
    expect(getByTestId("theme").textContent).toBe("dark");
    expect(getByTestId("resolved").textContent).toBe("dark");
  });

  it("sets data-theme on document element", () => {
    localStorage.setItem("gh-gantt-theme", "dark");
    render(
      <ThemeProvider>
        <TestConsumer />
      </ThemeProvider>,
    );
    expect(document.documentElement.dataset.theme).toBe("dark");
  });

  it("persists theme to localStorage on change", () => {
    const { getByTestId } = render(
      <ThemeProvider>
        <TestConsumer />
      </ThemeProvider>,
    );
    act(() => getByTestId("set-dark").click());
    expect(localStorage.getItem("gh-gantt-theme")).toBe("dark");
    expect(getByTestId("resolved").textContent).toBe("dark");
  });

  it("resolves system theme based on matchMedia", () => {
    vi.stubGlobal(
      "matchMedia",
      vi.fn().mockImplementation((query: string) => ({
        matches: true,
        media: query,
        addEventListener: (_: string, cb: (e: { matches: boolean }) => void) => {
          matchMediaListeners.push(cb);
        },
        removeEventListener: vi.fn(),
      })),
    );
    const { getByTestId } = render(
      <ThemeProvider>
        <TestConsumer />
      </ThemeProvider>,
    );
    expect(getByTestId("theme").textContent).toBe("system");
    expect(getByTestId("resolved").textContent).toBe("dark");
  });

  it("reacts to OS theme changes when set to system", () => {
    const { getByTestId } = render(
      <ThemeProvider>
        <TestConsumer />
      </ThemeProvider>,
    );
    expect(getByTestId("resolved").textContent).toBe("light");
    act(() => {
      for (const listener of matchMediaListeners) {
        listener({ matches: true });
      }
    });
    expect(getByTestId("resolved").textContent).toBe("dark");
  });
});
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `pnpm --filter @gh-gantt/ui exec vp test run src/__tests__/theme.test.tsx`
Expected: FAIL — `../contexts/ThemeContext.js` が存在しない

- [ ] **Step 3: ThemeContext 実装**

```tsx
// packages/ui/src/contexts/ThemeContext.tsx
import { createContext, useContext, useState, useEffect, useCallback, useMemo } from "react";

type Theme = "system" | "light" | "dark";
type ResolvedTheme = "light" | "dark";

interface ThemeContextValue {
  theme: Theme;
  resolvedTheme: ResolvedTheme;
  setTheme: (theme: Theme) => void;
}

const STORAGE_KEY = "gh-gantt-theme";

const ThemeContext = createContext<ThemeContextValue | null>(null);

function getSystemTheme(): ResolvedTheme {
  return window.matchMedia("(prefers-color-scheme: dark)").matches ? "dark" : "light";
}

function resolveTheme(theme: Theme): ResolvedTheme {
  return theme === "system" ? getSystemTheme() : theme;
}

function applyTheme(resolved: ResolvedTheme): void {
  if (resolved === "dark") {
    document.documentElement.dataset.theme = "dark";
  } else {
    delete document.documentElement.dataset.theme;
  }
}

export function ThemeProvider({ children }: { children: React.ReactNode }) {
  const [theme, setThemeState] = useState<Theme>(() => {
    const stored = localStorage.getItem(STORAGE_KEY);
    return stored === "light" || stored === "dark" ? stored : "system";
  });
  const [systemDark, setSystemDark] = useState(() => getSystemTheme() === "dark");

  const resolvedTheme: ResolvedTheme = theme === "system" ? (systemDark ? "dark" : "light") : theme;

  useEffect(() => {
    applyTheme(resolvedTheme);
  }, [resolvedTheme]);

  useEffect(() => {
    const mq = window.matchMedia("(prefers-color-scheme: dark)");
    const handler = (e: { matches: boolean }) => setSystemDark(e.matches);
    mq.addEventListener("change", handler);
    return () => mq.removeEventListener("change", handler);
  }, []);

  const setTheme = useCallback((next: Theme) => {
    setThemeState(next);
    localStorage.setItem(STORAGE_KEY, next);
  }, []);

  const value = useMemo(
    () => ({ theme, resolvedTheme, setTheme }),
    [theme, resolvedTheme, setTheme],
  );

  return <ThemeContext.Provider value={value}>{children}</ThemeContext.Provider>;
}

export function useTheme(): ThemeContextValue {
  const ctx = useContext(ThemeContext);
  if (!ctx) throw new Error("useTheme must be used within ThemeProvider");
  return ctx;
}
```

- [ ] **Step 4: テストが通ることを確認**

Run: `pnpm --filter @gh-gantt/ui exec vp test run src/__tests__/theme.test.tsx`
Expected: ALL PASS

- [ ] **Step 5: コミット**

```bash
git add packages/ui/src/contexts/ThemeContext.tsx packages/ui/src/__tests__/theme.test.tsx
git commit -m "feat(ui): ThemeProvider + useTheme hook (#dark-mode)"
```

---

### Task 2: index.html — ダーク CSS 変数 + FOUC 防止

**Files:**

- Modify: `packages/ui/index.html`

- [ ] **Step 1: FOUC 防止スクリプトを追加**

`<title>` の直後、`<style>` の前に以下を追加:

```html
<script>
  (function () {
    var t = localStorage.getItem("gh-gantt-theme") || "system";
    var dark =
      t === "dark" || (t === "system" && matchMedia("(prefers-color-scheme: dark)").matches);
    if (dark) document.documentElement.dataset.theme = "dark";
  })();
</script>
```

- [ ] **Step 2: `[data-theme="dark"]` CSS 変数ブロックを追加**

`:root { ... }` ブロックの閉じ括弧の直後に以下を追加:

```css
[data-theme="dark"] {
  /* ── Brand ── */
  --color-primary: #58a6ff;
  --color-primary-light: #79c0ff;
  --color-accent: #f0883e;

  /* ── Surfaces ── */
  --color-bg: #0d1117;
  --color-surface: #161b22;
  --color-border: #30363d;
  --color-border-light: #21262d;

  /* ── Text ── */
  --color-text: #c9d1d9;
  --color-text-secondary: #8b949e;
  --color-text-muted: #484f58;

  /* ── Semantic ── */
  --color-success: #3fb950;
  --color-danger: #f85149;
  --color-danger-dark: #da3633;
  --color-warning: #e3b341;
  --color-warning-dark: #d29922;
  --color-info: #58a6ff;
  --color-complete: #bc8cff;
  --color-in-progress: #3fb950;

  /* ── Semantic backgrounds ── */
  --color-danger-bg: rgba(248, 81, 73, 0.1);
  --color-warning-bg: rgba(227, 179, 65, 0.1);
  --color-info-bg: rgba(56, 139, 253, 0.1);
  --color-complete-bg: rgba(137, 87, 229, 0.1);
  --color-success-bg: rgba(63, 185, 80, 0.1);

  /* ── Interactive states ── */
  --color-selected-bg: rgba(56, 139, 253, 0.15);
  --color-selected-fg: #58a6ff;
  --color-selected-border: #1f6feb;
  --color-hover-bg: rgba(56, 139, 253, 0.08);
  --color-highlight-search: rgba(227, 179, 65, 0.3);

  /* ── Relation highlights ── */
  --color-highlight-parent-bg: rgba(137, 87, 229, 0.15);
  --color-highlight-parent-border: #bc8cff;
  --color-highlight-blocker-bg: rgba(248, 81, 73, 0.15);
  --color-highlight-blocker-border: #f85149;

  /* ── Drop states ── */
  --color-drop-reparent-bg: rgba(56, 139, 253, 0.15);
  --color-drop-reparent-border: #58a6ff;
  --color-drop-dependency-bg: rgba(240, 136, 62, 0.15);
  --color-drop-dependency-border: #f0883e;
  --color-drop-invalid-bg: rgba(248, 81, 73, 0.15);

  /* ── At-risk badge ── */
  --color-atrisk-bg: rgba(227, 179, 65, 0.2);
  --color-atrisk-fg: #e3b341;

  /* ── Priority ── */
  --color-priority-low-bg: #21262d;
  --color-priority-low-fg: #8b949e;
  --color-priority-low-border: rgba(139, 148, 158, 0.25);
}
```

- [ ] **Step 3: ビルド確認**

Run: `pnpm --filter @gh-gantt/ui exec vp build`
Expected: ビルド成功

- [ ] **Step 4: コミット**

```bash
git add packages/ui/index.html
git commit -m "feat(ui): ダーク CSS 変数 + FOUC 防止スクリプト (#dark-mode)"
```

---

### Task 3: ThemeToggle コンポーネント

**Files:**

- Create: `packages/ui/src/components/toolbar/ThemeToggle.tsx`
- Modify: `packages/ui/src/components/toolbar/Toolbar.tsx`

- [ ] **Step 1: ThemeToggle コンポーネント作成**

```tsx
// packages/ui/src/components/toolbar/ThemeToggle.tsx
import React from "react";
import { Sun, Moon, Monitor } from "lucide-react";
import { useTheme } from "../../contexts/ThemeContext.js";
import { IconButton } from "./IconButton.js";

const CYCLE: Array<"system" | "light" | "dark"> = ["system", "light", "dark"];
const LABELS: Record<string, string> = {
  system: "Theme: System",
  light: "Theme: Light",
  dark: "Theme: Dark",
};
const ICONS: Record<string, React.ReactNode> = {
  system: <Monitor size={14} />,
  light: <Sun size={14} />,
  dark: <Moon size={14} />,
};

export function ThemeToggle() {
  const { theme, setTheme } = useTheme();
  const next = CYCLE[(CYCLE.indexOf(theme) + 1) % CYCLE.length];

  return (
    <IconButton
      icon={ICONS[theme]}
      title={`${LABELS[theme]} (click → ${next})`}
      onClick={() => setTheme(next)}
    />
  );
}
```

- [ ] **Step 2: Toolbar に ThemeToggle を配置**

`packages/ui/src/components/toolbar/Toolbar.tsx` を編集:

import 追加:

```tsx
import { ThemeToggle } from "./ThemeToggle.js";
```

`<SyncGroup ... />` の直前（`<div style={{ flex: 1 }} />` の直後）に追加:

```tsx
<ThemeToggle />
```

- [ ] **Step 3: ビルド確認**

Run: `pnpm --filter @gh-gantt/ui exec vp build`
Expected: ビルド成功

- [ ] **Step 4: コミット**

```bash
git add packages/ui/src/components/toolbar/ThemeToggle.tsx packages/ui/src/components/toolbar/Toolbar.tsx
git commit -m "feat(ui): ThemeToggle ツールバーボタン (#dark-mode)"
```

---

### Task 4: App.tsx に ThemeProvider をラップ

**Files:**

- Modify: `packages/ui/src/App.tsx`

- [ ] **Step 1: ThemeProvider import 追加**

`packages/ui/src/App.tsx` の import セクションに追加:

```tsx
import { ThemeProvider } from "./contexts/ThemeContext.js";
```

- [ ] **Step 2: App コンポーネントの返り値を ThemeProvider でラップ**

`App()` の `return (` の直後の `<div style={{ height: "100%"...` を `<ThemeProvider>` でラップ:

```tsx
return (
  <ThemeProvider>
    <div style={{ height: "100%", display: "flex", flexDirection: "column" }}>
      {/* ... 既存のコンテンツすべて ... */}
    </div>
  </ThemeProvider>
);
```

- [ ] **Step 3: syncing オーバーレイのハードコード色を CSS 変数に置換**

同じ `App.tsx` 内の syncing オーバーレイ（L509-528 付近）:

変更前:

```tsx
background: "rgba(255,255,255,0.5)",
```

変更後:

```tsx
background: "rgba(128,128,128,0.3)",
```

変更前:

```tsx
color: "#555",
background: "#fff",
```

変更後:

```tsx
color: "var(--color-text-secondary)",
background: "var(--color-surface)",
```

- [ ] **Step 4: ビルド + テスト確認**

Run: `pnpm --filter @gh-gantt/ui exec vp build && pnpm --filter @gh-gantt/ui exec vp test run`
Expected: ビルド成功、テスト ALL PASS

- [ ] **Step 5: コミット**

```bash
git add packages/ui/src/App.tsx
git commit -m "feat(ui): ThemeProvider ラップ + ハードコード色修正 (#dark-mode)"
```

---

### Task 5: Toolbar のハードコード色を CSS 変数化

**Files:**

- Modify: `packages/ui/src/components/toolbar/Toolbar.tsx`

- [ ] **Step 1: Toolbar のインラインスタイルを修正**

`packages/ui/src/components/toolbar/Toolbar.tsx` の Toolbar コンポーネントのルート div スタイル:

変更前:

```tsx
borderBottom: "1px solid #e0e0e0",
background: "#fff",
```

変更後:

```tsx
borderBottom: "1px solid var(--color-border)",
background: "var(--color-surface)",
```

- [ ] **Step 2: ビルド確認**

Run: `pnpm --filter @gh-gantt/ui exec vp build`
Expected: ビルド成功

- [ ] **Step 3: コミット**

```bash
git add packages/ui/src/components/toolbar/Toolbar.tsx
git commit -m "fix(ui): Toolbar ハードコード色を CSS 変数化 (#dark-mode)"
```

---

### Task 6: 主要コンポーネントのハードコード色修正

**Files:**

- Modify: `packages/ui/src/components/ShortcutHelpPanel.tsx`
- Modify: `packages/ui/src/components/AssigneeFilter.tsx`
- Modify: `packages/ui/src/components/PriorityFilter.tsx`
- Modify: `packages/ui/src/components/LabelFilter.tsx`
- Modify: `packages/ui/src/components/TypeFilter.tsx`
- Modify: `packages/ui/src/components/TaskRow.tsx`
- Modify: `packages/ui/src/components/MarkdownEditor.tsx`

この Task は広範囲だが、すべて同じパターンの変更（`"#fff"` → `"var(--color-surface)"`, `"#333"` → `"var(--color-text)"` 等）。各コンポーネントの変更を以下に列挙する。

- [ ] **Step 1: フィルター系コンポーネントの共通パターン修正**

`AssigneeFilter.tsx`, `PriorityFilter.tsx`, `LabelFilter.tsx` に共通する修正パターン:

| Before                      | After                        |
| --------------------------- | ---------------------------- |
| `background: ... "#fff"`    | `"var(--color-surface)"`     |
| `color: ... "#333"`         | `"var(--color-text)"`        |
| `color: ... "#1a73e8"`      | `"var(--color-selected-fg)"` |
| `background: ... "#e8f0fe"` | `"var(--color-selected-bg)"` |
| `background: ... "#f0f4ff"` | `"var(--color-hover-bg)"`    |

各ファイルで上記のハードコード色を CSS 変数に置換する。

- [ ] **Step 2: TypeFilter.tsx の修正**

| Before                      | After                            |
| --------------------------- | -------------------------------- |
| `border: ... "#c5d7f7"`     | `"var(--color-selected-border)"` |
| `border: ... "#ddd"`        | `"var(--color-border)"`          |
| `background: ... "#e8f0fe"` | `"var(--color-selected-bg)"`     |
| `background: ... "#fff"`    | `"var(--color-surface)"`         |

- [ ] **Step 3: TaskRow.tsx の修正**

| Before                                    | After                                    |
| ----------------------------------------- | ---------------------------------------- |
| `color: "#fff"` (badge)                   | そのまま維持（バッジ色のコントラスト用） |
| `color: "#2c3e50"`                        | `"var(--color-text)"`                    |
| `background: "rgba(255, 255, 255, 0.98)"` | `"var(--color-surface)"`                 |

- [ ] **Step 4: MarkdownEditor.tsx の修正**

| Before                        | After                         |
| ----------------------------- | ----------------------------- |
| `color: "#b45309"` (Unsaved)  | `"var(--color-warning-dark)"` |
| `color: "#fff"` (save button) | そのまま維持                  |

- [ ] **Step 5: ShortcutHelpPanel.tsx の修正**

ShortcutHelpPanel は独特のスタイリング（暖色系和風テーマ）を持つが、背景色をダークモード対応にする:

| Before                     | After                           |
| -------------------------- | ------------------------------- |
| `background: "#fffdf8"`    | `"var(--color-surface)"`        |
| `background: "#fff"` (kbd) | `"var(--color-bg)"`             |
| `background: "#f7efe3"`    | `"var(--color-hover-bg)"`       |
| `color: "#8a6b43"`         | `"var(--color-text-muted)"`     |
| `color: "#2d2418"`         | `"var(--color-text)"`           |
| `color: "#7b6f62"`         | `"var(--color-text-secondary)"` |
| `color: "#4a3f33"`         | `"var(--color-text)"`           |
| `color: "#6e5330"`         | `"var(--color-text-secondary)"` |

- [ ] **Step 6: ビルド + テスト確認**

Run: `pnpm --filter @gh-gantt/ui exec vp build && pnpm --filter @gh-gantt/ui exec vp test run`
Expected: ビルド成功、テスト ALL PASS

- [ ] **Step 7: コミット**

```bash
git add packages/ui/src/components/ShortcutHelpPanel.tsx \
  packages/ui/src/components/AssigneeFilter.tsx \
  packages/ui/src/components/PriorityFilter.tsx \
  packages/ui/src/components/LabelFilter.tsx \
  packages/ui/src/components/TypeFilter.tsx \
  packages/ui/src/components/TaskRow.tsx \
  packages/ui/src/components/MarkdownEditor.tsx
git commit -m "fix(ui): 各コンポーネントのハードコード色を CSS 変数化 (#dark-mode)"
```

---

### Task 7: ビルド + 全テスト + lint 最終確認

**Files:** なし（検証のみ）

- [ ] **Step 1: 全体ビルド**

Run: `pnpm build`
Expected: 全パッケージビルド成功

- [ ] **Step 2: 全テスト**

Run: `pnpm test`
Expected: ALL PASS

- [ ] **Step 3: lint**

Run: `pnpm lint`
Expected: 新規 error なし

- [ ] **Step 4: dev サーバーで目視確認**

Run: `pnpm dev`

確認項目:

1. ツールバー右端にテーマ切り替えアイコンが表示される
2. クリックで System → Light → Dark と巡回する
3. ダークモードで背景・テキスト・ボーダーが GitHub Dark の配色になる
4. ガントバーの色はライトモードと同じ
5. フィルター・ポップオーバー・詳細パネルがダーク対応
6. ページリロード後もテーマが保持される
